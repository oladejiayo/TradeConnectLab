# TradeConnectLab
A self-contained platform that connects to multiple FIX counterparties/exchanges, performs migration from legacy sessions to new sessions/adapters, normalizes messages via a mapping engine, distributes events over Solace, and provides ops tooling + observability + CI/CD + certification testing.
